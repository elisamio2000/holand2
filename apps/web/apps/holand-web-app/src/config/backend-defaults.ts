/**
 * Backend connection defaults — no hardcoded service hosts in UI types.
 * Override via env or user input in admin wizards.
 */

/** Neutral default port hint for external LLM endpoint wizard (gateway standard). */
export const EXTERNAL_ENDPOINT_DEFAULT_PORT = 8000;
