/**
 * RizzUI `Modal` `customSize` (max-width) for expanded chat previews.
 * Inner `ResizableModalShell` sets actual width/height; this caps the modal wrapper.
 */
export const CHAT_EXPAND_MODAL_CUSTOM_SIZE = 'min(96vw, 980px)';
