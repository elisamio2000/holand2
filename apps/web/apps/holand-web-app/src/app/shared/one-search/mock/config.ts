// Re-export — legacy imports from ./mock/config still work.
export {
  getAllLaneSourceModes,
  getLaneSourceMode,
  getOneSearchProviderId,
  isOneSearchMockEnabled,
  ONE_SEARCH_DEFAULT_LIMIT,
  ONE_SEARCH_SOURCE_TIMEOUT_MS,
} from '../config/search-config';
